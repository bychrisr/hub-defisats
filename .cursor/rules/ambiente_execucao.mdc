---
description: Instruções sobre o ambiente de execução Docker.
globs: []
alwaysApply: true
---

## 🐳 Ambiente de Execução (Docker)

**Você obrigatoriamente deve rodar `docker compose` no projeto e nunca, em hipótese alguma rodar nativamente o stack do projeto. Estamos usando o ambiente de desenvolvimento.**

### Estrutura de Configuração

- **Compose File:** `config/docker/docker-compose.dev.yml`
- **Environment File:** `config/env/.env.development`

### Comandos Padrão

1. **Subir o ambiente:**
   ```bash
   cd ${CURSOR_PROJECT_PATH} && docker compose -f config/docker/docker-compose.dev.yml up -d
   ```

2. **Aguardar e verificar saúde dos serviços (exemplo com sleep):**
   ```bash
   sleep 15 && docker compose -f config/docker/docker-compose.dev.yml ps
   ```

3. **Testar API (exemplo):**
   ```bash
   curl -s "http://localhost:13010/api/version" | jq .
   ```

4. **Testar Frontend (exemplo):**
   ```bash
   curl -s -I "http://localhost:13000" | head -5
   ```

5. **Verificar status final:**
   ```bash
   docker compose -f config/docker/docker-compose.dev.yml ps
   ```

### Regras

- **Sempre utilize os caminhos e arquivos acima para controle do ambiente.**
- **Sempre espere um tempo razoável (usando `sleep`) após subir os serviços antes de tentar interagir com eles.**
- **Não execute serviços diretamente fora do Docker Compose.**